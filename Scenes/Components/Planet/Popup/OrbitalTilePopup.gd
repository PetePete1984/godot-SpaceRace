extends Container
# Orbital Square
# Orbital Square desc

# Build / OK (no idle pop) / Cancel (idle pop)

onready var label = get_node("TextureFrame/Content/Label")
onready var buttons = get_node("TextureFrame/Buttons")